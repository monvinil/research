# API Keys — copy to .env and fill in
# All free except SERPER (optional, $50/mo)

# Anthropic — get key at https://console.anthropic.com/settings/keys
# Required for automated agent pipeline (Agent A/B/C/Master)
# Sign up at console.anthropic.com → Settings → API Keys → Create Key
# Then add credits at Settings → Billing (pay-as-you-go, ~$15-20/mo estimated)
ANTHROPIC_API_KEY=

# FRED — get free key at https://fred.stlouisfed.org/docs/api/api_key.html
FRED_API_KEY=

# BLS — works without key (rate limited to 25/day). Get free key at https://data.bls.gov/registrationEngine/
BLS_API_KEY=

# BEA — get free key at https://apps.bea.gov/api/signup/
# GDP by industry, input-output tables, services trade, regional GDP
BEA_API_KEY=

# Census Bureau — get free key at https://api.census.gov/data/key_signup.html
# Business Dynamics Statistics, County Business Patterns, establishment counts
CENSUS_API_KEY=

# SEC EDGAR — no key needed, but requires contact email for User-Agent
EDGAR_CONTACT_EMAIL=your@email.com

# Web search — optional. DuckDuckGo works free with no key.
# If you want higher volume/reliability, get Serper key at https://serper.dev
SERPER_API_KEY=

# World Bank — no key needed (open data)
# OECD — no key needed (SDMX REST API)
